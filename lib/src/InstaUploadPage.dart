import 'package:flutter/material.dart';
import 'package:get/get.dart';

class InstaUploadPage extends StatelessWidget {
  const InstaUploadPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text('UploadPage')),
    );
  }
}
